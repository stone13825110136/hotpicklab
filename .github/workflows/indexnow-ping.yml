name: Ping IndexNow

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: node scripts/ping-indexnow.mjs
